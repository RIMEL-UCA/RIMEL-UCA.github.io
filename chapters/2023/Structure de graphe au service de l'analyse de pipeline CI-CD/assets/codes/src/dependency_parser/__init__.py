## templates :

# detect if the current pattern exist inside the workflow
def detect(yml):
    ...
# append to the map a new dependency model
def links_dependencies(yml, job_map):
    ...
# transform object data into a graph representation
def build_graph(dot, dep):
    ...


